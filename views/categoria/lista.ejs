<script src="/js/categoria/lista.js"></script>

<div class="page-card">

    <div class="page-header">

        <h1>Categorias</h1>

        <a href="/categoria/cadastrar" class="btn btn-primary">
            <i class="fas fa-plus"></i>
            Nova Categoria
        </a>

    </div>

    <div class="table-responsive tabela-scroll">

        <table class="table table-striped table-hover align-middle tabela-sistema">

            <thead>
                <tr>
                    <th>ID</th>
                    <th>Descrição</th>
                    <th>Ativo</th>
                    <th>Ações</th>
                </tr>
            </thead>

            <tbody>

                <% for(let i = 0; i < lista.length; i++) { %>

                    <tr>

                        <td>
                            <%= lista[i].catId %>
                        </td>

                        <td>
                            <%= lista[i].catDescricao %>
                        </td>

                        <td>

                            <% if(lista[i].catAtivo == 's') { %>

                                <span class="badge bg-success">
                                    Sim
                                </span>

                            <% } else { %>

                                <span class="badge bg-danger">
                                    Não
                                </span>

                            <% } %>

                        </td>

                        <td>

                            <div class="acoes">

                                <a
                                    href="/categoria/alterar/<%= lista[i].catId %>"
                                    class="btn btn-primary btn-sm"
                                >
                                    <i class="fas fa-pen"></i>
                                </a>

                                <button
                                    data-id="<%= lista[i].catId %>"
                                    class="btn btn-danger btn-sm btnExcluir"
                                >
                                    <i class="fas fa-trash"></i>
                                </button>

                            </div>

                        </td>

                    </tr>

                <% } %>

            </tbody>

        </table>

    </div>

</div>